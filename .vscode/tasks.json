{
    "tasks": [
      {
        "label": "build_remote_compaction",
        "type": "shell",
        "command": "ninja",
        "options": {
          "cwd": "${workspaceFolder}/build"
        }
      }
    ],
    "version": "2.0.0"
  }